# Copiez ce fichier en .env et remplacez par vos valeurs
# Ne commitez jamais .env

# URI MongoDB (Atlas / cluster)
MONGODB_URI=mongodb+srv://USER:PASSWORD@HOST/DBNAME?retryWrites=true&w=majority

# Port de l’API (optionnel)
PORT=8010
